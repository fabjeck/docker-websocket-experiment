{
  "name": "websocket-tutorial",
  "version": "1.0.0",
  "description": "a demo chat app with socket.io",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --experimental-modules index.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
