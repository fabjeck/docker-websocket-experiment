version: '3'

services:
  nodejs:
    build:
      context: ./socketio-app
      dockerfile: Dockerfile
    image: nodejs
    container_name: nodejs
    restart: unless-stopped
    networks:
      - app-network

  webserver:
    image: nginx:mainline-alpine
    container_name: webserver
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - web-root:/var/www/html
      - ./nginx-conf:/etc/nginx/conf.d
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
    depends_on:
      - nodejs
    networks:
      - app-network

  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes:
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
      - web-root:/var/www/html
    depends_on:
      - webserver
    command: certonly --webroot --webroot-path=/var/www/html --email fabien.jeckelmann@stud.hslu.ch --agree-tos --no-eff-email --force-renewal -d docker.fabien-jeckelmann.me

volumes:
  certbot-etc:
  certbot-var:
  web-root:
  dhparams:
    driver: local
    driver_opts:
      type: none
      device: /home/fabienjeckelmann/socketio-app/dhparams/
      o: bind

networks:
  app-network:
    driver: bridge